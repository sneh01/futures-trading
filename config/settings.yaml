risk:
  risk_per_trade: 0.1
  risk_to_reward: 2 # 2x reward for risk so take profit at 10 ticks
  stop_loss_ticks: 5 # stop loss at 5 ticks for MES
  max_position_size: 10
  max_drawdown: 0.2

exec:
  mode: backtest
  broker: mock
  symbol: ES
  tick_value: 1.5
  tick_size: 0.25

strategy:
  name: rsi_cooldown
  params:
    rsi_period: 14
    rsi_entry: 45
    cooldown_bars: 100


risk:
  risk_per_trade: 0.01
  risk_to_reward: 2.0
  stop_loss_ticks: 20
  max_position_size: 5
  max_drawdown: 0.2

exec:
  mode: backtest
  broker: mock
  symbol: ES
  tick_value: 12.5
  tick_size: 0.25

strategy:
  name: simple_random
  params:
    ema_fast: 9
    ema_slow: 21
    rsi_period: 14
    rsi_entry_threshold: 60

